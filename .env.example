# Base application URL (used for SEO metadata, Open Graph, Sitemap, and Robots.txt).
# Must be an absolute https:// URL in production; falls back to VERCEL_URL, then
# a hardcoded production default, if unset (see helpers/site.ts).
APP_URL=http://localhost:3000
